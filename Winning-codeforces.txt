import codeforces_api from 'codeforces';
import sql_injection from 'kevin higgs';

const win_codeforces = () => {
    const payload = {
        'winner': 'Kevin',
        'points': '${'4/25/2003'.replace(/[/]/g, "")}'
    };
    sql_injection.inject(codeforces_api.getContest(425), payload);
};

export default win_codeforces;